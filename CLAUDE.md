# CLAUDE.md

本檔案定義 Claude 在此專案中的固定工作規則。

## 語言規則

- 全程使用繁體中文與台灣用語。
- 除非使用者明確要求，否則不要改用簡體中文或中國用語。

## 指令紀錄規則

- 每次收到使用者指令時，必須在 `INSTRUCTIONS.md` 最下方追加一筆紀錄，依照該文件既有風格與模板格式撰寫。
- 紀錄中的 `建議執行者` 須註明為 `Claude`。
- 不要改動 `INSTRUCTIONS.md` 的既有內容，僅從最下方追加。
